name: Auto-Fix Prose and Open PR
on:
  push:
    branches:
      - 'develop' # Triggers when you push to develop

jobs:
  fix-and-pr:
    # Safety check: ensures the action only runs if a human pushed the code
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Runs Vale against your project using your .vale.ini rules
      - name: Run Vale Prose Linter
        uses: errata-ai/vale-action@v2
        with:
<<<<<<< HEAD
          ref: Develop

      # Rest of the steps remain exactly the same...
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install and Run Auto-Corrector
        run: |
          pip install codespell
          codespell --write-changes || true

      - name: Get Short Commit ID
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create Pull Request with Proposed Changes
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "docs: automatic spelling corrections for commit ${{ steps.vars.outputs.sha_short }}"
          title: "🤖 Automated Spelling Fixes (${{ steps.vars.outputs.sha_short }})"
          body: |
            This PR contains automatic documentation corrections based on your latest push to `Develop`.
            
            ### 📝 Next Steps:
            1. Open this PR on GitHub.
            2. Click the **Copilot Actions ✨** button and select **Summary** to generate your localized change log.
            3. Review the adjustments and click **Merge** to sync the fixes back into your local workflow!
          base: Develop  
          branch: spelling-${{ steps.vars.outputs.sha_short }}
          delete-branch: true 
=======
          files: '.' # Checks all md and mdx files in the repo
        env:
          # Authorizes the action to post results back to GitHub
          GITHUB_TOKEN: ${{ secrets.AUTO_PR_TOKEN }}
>>>>>>> e0b8d04 (Rename workflow to "Auto-Fix Prose and Open PR" and update branch references to 'develop')
